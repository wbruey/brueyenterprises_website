if loaded onto the server, this will add one entry to the database.


dependencies:

../firebase/brutest-3228a-firebase-adminsdk-bex36-272252f791.json    this is the sdk cert key from firebase download from firebase console - settings - service accounts tab generate new private key